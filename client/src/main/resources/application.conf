{
  "application" {
    "username" = ${?CHAT_USER}
    "password" = ${?CHAT_PASSWORD}

    "server" {
      "host" = "127.0.0.1"
      "host" = ${?CHAT_SERVER_HOST}
      "port" = "8080"
      "port" = ${?CHAT_SERVER_PORT}
    }
  }
}